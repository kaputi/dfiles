package core

func Remove(path string) {
	// // Remove the file or directory at the given path
	// cmd := gitCmd("rm", path)
	// if cmd != nil {
	// 	panic("Error removing file: " + string(cmd))
	// }
}
